# Server Configuration
PORT=5000

# Admin Credentials
ADMIN_USER=admin
ADMIN_PASS=nathji2026

# Email Authentication (Primary: works automatically with Gmail if SMTP_HOST is blank)
EMAIL_USER=
EMAIL_PASS=

# Advanced SMTP Configuration (Optional)
# If SMTP_HOST is set, it overrides the default Gmail transport
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
